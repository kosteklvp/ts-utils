describe('@Memoize()', () => {
    // it('should cache the result of a static method', () => {
    //     const spy = jest.spyOn(TestClass, 'multiply');
    //     expect(TestClass.multiply(2, 3)).toBe(6);
    //     expect(TestClass.multiply(2, 3)).toBe(6);
    //     expect(TestClass.multiply(3, 4)).toBe(12);
    //     expect(spy).toHaveBeenCalledTimes(2); // Called once for each unique set of arguments
    // });
});
